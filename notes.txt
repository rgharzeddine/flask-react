docker-compose up -d 
    did not started
    docker had problems mounting using ADD
solution
    used commands
        docker rm users-service
        docker run -d -p 5001:5000 --name users-service -e APP_SETTINGS=project.config.DevelopmentConfig flaskmicroservicesusers_users-service
